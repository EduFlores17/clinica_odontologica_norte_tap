---
const fondoHero = "/dentista.jpg"; // Imagen de fondo profesional (ej: consultorio o sonrisa)
const paciente = "/n.jpg"; // Tu imagen de paciente
---

<section class="relative w-full overflow-hidden min-h-[600px] flex items-center">
  <!-- Imagen de fondo con overlay oscuro -->
  <div class="absolute inset-0 z-0">
    <img 
      src={fondoHero} 
      alt="Consultorio dental moderno" 
      class="h-full w-full object-cover object-center"
      width="1920" 
      height="1080"
    />
    <!-- Overlay para contraste del texto -->
    <div class="absolute inset-0 bg-blue-900/70"></div>
  </div>

  <!-- Contenido (mismo contenedor que el header) -->
  <div class="relative z-10 mx-auto flex max-w-6xl flex-col items-center justify-between gap-10 px-4 py-16 md:flex-row md:gap-6 md:px-6 lg:py-24">
    
    <!-- Lado izquierdo: Texto -->
    <div class="max-w-2xl text-center md:text-left">
      <h1 class="mb-6 text-4xl font-bold leading-tight text-white sm:text-5xl md:text-6xl">
        <span class="bg-gradient-to-r from-cyan-300 to-white bg-clip-text text-transparent">
          Sonrisas
        </span> 
        <span class="text-cyan-300">que inspiran</span> confianza
      </h1>

      <p class="mb-8 text-lg font-light leading-relaxed text-blue-100 md:text-xl">
        Tecnología de vanguardia y un enfoque humano para cuidar de tu salud dental.
      </p>

      <div class="flex flex-col items-center gap-4 sm:flex-row sm:justify-start">
        <a href="#servicios" class="transform rounded-full bg-cyan-400 px-8 py-3 text-lg font-semibold text-blue-900 shadow-lg transition-all hover:scale-105 hover:bg-cyan-300 hover:shadow-cyan-300/30">
          Nuestros servicios
        </a>
        
        <a href="#servicios" class="transform rounded-full bg-cyan-400 px-8 py-3 text-lg font-semibold text-blue-900 shadow-lg transition-all hover:scale-105 hover:bg-cyan-300 hover:shadow-cyan-300/30">
          Contactanos
        </a>
      </div>
    </div>

    <!-- Lado derecho: Imagen circular (mejorada) -->
    <div class="relative">
      <!-- Efecto de sombra 3D -->
      <div class="absolute -inset-4 rounded-full bg-gradient-to-br from-blue-700/60 to-cyan-500/60 blur-xl"></div>
      
      <!-- Marco con borde sutil -->
      <div class="relative overflow-hidden rounded-full border-[6px] border-white/30 shadow-2xl">
        <img 
          src={paciente} 
          alt="Paciente feliz en clínica dental" 
          class="h-64 w-64 object-cover object-center md:h-80 md:w-80"
          width="400"
          height="400"
        />
      </div>
      
    
    </div>
  </div>
</section>